import { atom } from "nanostores";

export const userSpecificAssetsDisplayStatus = atom(
  "userSpecificAssetsNotDisplayed"
);
export const selectedFileContentDisplayStatus = atom(
  "selectedFileContentDisplayed"
);
export const saveButtonDisplayStatus = atom(
  "saveButtonNotDisplayed"
);
